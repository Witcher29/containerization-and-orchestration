## Часть 2

### Запуск сервиса api в собственном namespace. 
*sudo unshare --pid --mount --net --uts --ipc --user --map-root-user --fork --mount-proc ./api*
- Флаг pid задаёт процессу PID=1, т.е. думает сервис api думает, что стал root'ом.
- Флаг mount изолирует файловую системы
- Флаг net создаёт lo
- Флаг uts даёт возможность задать hostname в контейнере
- Флаг ipc выносит в своё пространство медпроцессное взаимодействие для контейнера
- Флаг user задаёт изолированное пространство имён пользователей и групп.
- Флаг map-root-user сделает так, что внутри контейнера процесс, выполняемый от имени root, в реальной машине будет выполняться от имени пользователя
- Флаг fork создаёт дочерний процесс, внутри которого и будет выполняться сервис api
- Флаг mount-proc нужен, чтобы внутри изолированной среды утилиты мониторинга не видели процессов за пределами контейнера

Запуск команды представлен на рисунке снизу:
<img width="928" height="67" alt="image" src="https://github.com/user-attachments/assets/64adf1b1-86bb-44e1-a14f-d41694660849" />
Для успешного запуска от не root пользователя пришлось снять защиту от несанкционированного создания пользовательских пространств имён:

*sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0*

В результате выполнения команды получаем процесс, находящийся в собственном namespace. Внутри этого namespace процесс сервиса api работает от имени root, также имеет собственную сеть, hostname (если задать отличный от оригинального) и область видимости средств мониторинга (ps aux, запущенный внутри namespace не показывает процессы хоста):

<img width="917" height="453" alt="image" src="https://github.com/user-attachments/assets/0199e5f8-dfc6-4756-8048-462f0f110d43" />

Демонстрация сети хоста для сравнения с сетью контейнера на предыдущем скриншоте:
<img width="797" height="275" alt="image" src="https://github.com/user-attachments/assets/7faa04df-ed73-469e-8fc2-75e69c466610" />

## Часть 3
### Проверка превышения лимита по памяти
Мы решили проверить, как система отреагирует на нехватку оперативной памяти. Для этого мы создали новую контрольную группу lab-api и установили для неё жесткий лимит памяти, записав значение 64M в файл memory.max. После проверки через cat мы убедились, что лимит применился и составляет 67108864 байт (ровно 64 мегабайта). Затем мы поместили наш процесс с PID 280470 внутрь этой cgroup, чтобы все его потребления памяти ограничивались этими рамками.
Чтобы проверить лимит в действии, мы обратились к нашей ручке /eat с параметром выделения 6000 мегабайт (mb=6000). Поскольку наш процесс изолирован в собственном сетевом пространстве имен, мы использовали утилиту nsenter, чтобы выполнить curl как бы "изнутри" этого процесса, обращаясь к локальному хосту. Мы отправили запрос на http://127.0.0.1:8080/eat?mb=6000. Запрос на выделение 6 ГБ при лимите в 64 МБ, естественно, не мог быть выполнен. В ответ мы получили ошибку curl: (52) Empty reply from server. Это стало первым сигналом того, что процесс не справился с нагрузкой и, скорее всего, был аварийно завершен ядром.

<img width="947" height="238" alt="image" src="https://github.com/user-attachments/assets/be31078b-50b6-458b-9159-7443657023c1" />

Чтобы убедиться в наших догадках, мы сначала проверили статус процесса командой ps -p 280470. Вывод был пуст — это означало, что процесс больше не существует. После этого мы заглянули в файл статистики memory.events, чтобы понять причину смерти. Мы заметили, что счётчик max достиг значения 15292, то есть процесс множество раз упирался в установленный лимит при попытке выделить память. Самое главное, что мы увидели — это счетчики oom 1 и oom_kill 1. Это подтвердило, что сработал механизм OOM (Out Of Memory) killer: когда процесс попытался превысить доступные ему 64 МБ, ядро Linux принудительно убило его, чтобы защитить систему от зависания.

<img width="654" height="241" alt="image" src="https://github.com/user-attachments/assets/62f5f06f-57b2-4ed7-9e4f-9c4b9758f808" />

### CPU Throttling

Мы начали с создания новой контрольной группы для нашего API. Для этого мы выполнили команду sudo mkdir /sys/fs/cgroup/lab-api-cpu. Затем мы установили ограничение на использование процессорного времени: записали строку "50000 100000" в файл cpu.max. Это означает, что на каждый период в 100000 микросекунд (100 мс) нашему процессу разрешено использовать только 50000 микросекунд (50 мс), то есть ровно половину одного ядра. После этого мы проверили, что лимит применился, выполнив cat, и убедились, что значения корректны. Далее мы поместили наш процесс с PID 4925 внутрь этой cgroup, записав его идентификатор в файл

<img width="974" height="153" alt="image" src="https://github.com/user-attachments/assets/11659e45-55fd-4890-8f8a-1c66fcb3c59c" />

Перед тем как запустить нагрузку, мы решили посмотреть на исходную статистику процессора в нашей cgroup. Мы выполнили cat /sys/fs/cgroup/lab-api-cpu/cpu.stat. В выводе мы заметили, что счётчик nr_periods равен 4, а счётчики nr_throttled и throttled_usec пока находятся на нуле. Это ожидаемо, так как процесс ещё не начал активно потреблять CPU, и троттлинга (принудительной остановки) не происходило

<img width="798" height="238" alt="image" src="https://github.com/user-attachments/assets/053f3b0a-f9c5-415c-a810-5f76ecf789c5" />

Чтобы проверить наш лимит в действии, мы обратились к нашему API через браузер по адресу http://192.168.133.129:8080/burn. Мы дернули ручку /burn, которая, как мы знаем, запускает бесконечный цикл, нагружающий одно ядро процессора. На экране мы увидели ответ burning, что подтвердило успешный старт нагрузки.

<img width="492" height="132" alt="image" src="https://github.com/user-attachments/assets/702fd4c7-6b27-4b82-911c-1a20cb51e8c1" />

Спустя некоторое время мы снова заглянули в файл статистики cpu.stat, чтобы оценить, как отработал наш лимит. Мы сразу заметили кардинальные изменения: количество периодов (nr_periods) выросло до 379, а счётчик nr_throttled подскочил до 375. Это означает, что в 375 из 379 периодов наш процесс упирался в установленный лимит и был вынужден останавливаться. Показатель throttled_usec также значительно вырос и составил 20541837 микросекунд (около 20.5 секунд). Мы убедились, что механизм cgroups работает корректно: искусственное ограничение в 50% ядра успешно сдерживает наш процесс, вызывая ожидаемый троттлинг.
<img width="778" height="244" alt="image" src="https://github.com/user-attachments/assets/01da891f-42be-4e96-b851-4c80762710d0" />

### Форк-бомба

Сначала проверяем текущее состояние cgroup lab-api-pids: значение pids.current показывает количество активных процессов, а pids.events позволяет увидеть случаи, когда система не смогла создать новый процесс из-за ограничения. Затем проверяем значение pids.max, которое установлено в 10. После этого запускаем stress-ng с параметрами --fork 50 --timeout 10s. Таким образом, программа пытается создавать большое количество дочерних процессов, а cgroup должна не позволить превысить установленный предел.
<img width="974" height="365" alt="image" src="https://github.com/user-attachments/assets/dd735c91-b693-444f-a214-a2b3cc05d893" />
Во время выполнения нагрузки значение pids.current достигает 10, то есть установленного максимума. При этом счётчик max в pids.events увеличивается и показывает большое количество попыток создать дополнительные процессы после достижения лимита. Это наглядно показывает работу механизма cgroups: несмотря на то, что stress-ng пытается создать до 50 процессов, количество одновременно существующих tasks в данной cgroup не превышает заданное значение 10.

<img width="972" height="107" alt="image" src="https://github.com/user-attachments/assets/11091f2a-e1a0-4afd-b41c-1117aaf37eca" />

## Часть 4
### Ограничение с помощью привилегий
Для начала с помощью следующей команды запретим внутри нашего контейнера дополнительные возможности с помощью ограничения привилегий:

*unshare --pid --mount --net --uts --ipc --user --map-root-user --fork \
  --mount-proc \
  setpriv --inh-caps=-all --ambient-caps=-all --bounding-set=-all \
    ./api*

<img width="958" height="136" alt="image" src="https://github.com/user-attachments/assets/739f5a67-064a-40d1-9c6c-1a0c467de01b" />

Эта команда помимо оговорённого в части 2 запускает процесс с указанными привилегиями, в данном случае с указанием отсутствия привилегий (у всех атрибутов стоит выключение). setpriv не запускает отдельный процесс, а подменяет свой процессом api (execve).
- Атрибут inh-caps влияет на передачу набора привилегий процессам-потомкам при exeve
- Атрибут ambient-caps делает так, что никакой набор привилегий не сохранится при exeve
- Атрибут bounding-set в данном случае навсегда лишает процесс шанса на получение какой-то привелегии даже от root'а

На картинке снизу демонстрируется, что пользователь внутри процесса не может изменить время, потому что не имеет на это прав.

<img width="960" height="278" alt="image" src="https://github.com/user-attachments/assets/e44ad9d9-d5ab-4bdf-8ecc-76ef75ae09d8" />

### Блокировка системного вызова
С помощью команды ниже мы помимо вышеописанного запрещаем изнутри контейнера делать системный вызов, отвечающий за создание директории:
*systemd-run --user --wait --pty   -p SystemCallFilter="~mkdir"   -p SystemCallErrorNumber=EPERM   unshare --pid --mount --net --uts --ipc --user --map-root-user --fork   --mount-proc   setpriv --inh-caps=-all --ambient-caps=-all --bounding-set=-all /home/alexandr/Desktop/containerization-labs/lab1/api*

<img width="958" height="141" alt="image" src="https://github.com/user-attachments/assets/928cfdd3-6804-4988-b2fe-f953295d7fb0" />

- Флаг user запускает модуль от имени пользовательского менеджера systemd (user@UID.service), а не системного. Не нужен root. Настройки применяются в рамках сессии.
- Флаг wait задаёт ожидание выполнения команды
- Флаг pty подключает псевдотерминал (PTY) — чтобы вывод команды шёл в ваш терминал в реальном времени, как при обычном запуске.
- Seccomp фильтр SystemCallFilter="~mkdir" запрещает вызов mkdir
- SystemCallErrorNumber=EPERM указывает, что при вызове запрещённого вызова нужно вернуть ошибку Operation not permitted

При попытке создать папку получаем указанную ошибку:
<img width="956" height="416" alt="image" src="https://github.com/user-attachments/assets/c40196a9-68f6-4aab-a10b-881ff923cc91" />

## Часть 5
### Скрипт mydocker.sh

Сам скрипт представлен в одноимённом файле.
Запуск скрипта:

<img width="748" height="69" alt="image" src="https://github.com/user-attachments/assets/f8e8ae8c-40b6-4a9b-8b3b-565d6e6bb403" />

Проверка работоспособности:

<img width="920" height="352" alt="image" src="https://github.com/user-attachments/assets/693722c3-56dd-4316-9fc0-cdd3b703a991" />

Для скрипта использовали команды из предыдущих шагов, но из-за того, для активации lo внутри процесса сделали systemd фоновым, не блокирующим ввод последующих команд.

### Сравнение с docker run

Проверка работоспособности:

<img width="1061" height="402" alt="image" src="https://github.com/user-attachments/assets/9399b87d-2852-4755-b8e3-7a168da2e04c" />

Docker перенаправляет запрос по порту 8080 сразу в соответствующий порт контейнера, nsenter не требуется, запрос отправляется сразу на localhost.

## Часть 6
### Dockerfile

В Dockerfile описана vulti-stage сборка, а в Dockerfile.single с минимальной конфигурацией.

Разницу между ними можно посмотреть на скриншоте снизу:

<img width="928" height="399" alt="image" src="https://github.com/user-attachments/assets/9cf65523-7f9e-4a42-a5eb-1e6df0848909" />

Многослойная сборка весит 4 Мб против 93 Мб у сборки с минимальной конфигурацией. К тому же образ многослойной сборки содержит только 3 слоя, а не 15.

### Файл в контейнере и в томе

Здесь демонстрируется, что файл, добавленный в контейнер удалился вместе с самим контейнером:

<img width="929" height="390" alt="image" src="https://github.com/user-attachments/assets/eabde219-9f82-42d6-aae0-ae9129ea8451" />

Здесь демонстрируется, что при сохранении в volume файл пережил удаление контейнера:

<img width="926" height="462" alt="image" src="https://github.com/user-attachments/assets/eed1f609-e1d0-43b4-bf77-fb8aa6db23cc" />

## Часть 7 gVisor

В этой части нужно было запустить ранее созданный Docker-образ с API через **gVisor (`runsc`)** и сравнить такой запуск с обычным Docker и с собственным способом запуска контейнера из предыдущих частей.

Также нужно было разобраться:

- как запускается контейнер через gVisor;
- чем gVisor отличается от обычного Docker runtime;
- почему gVisor обеспечивает дополнительный уровень изоляции;
- что остаётся общим между контейнером и хостом;
- почему общее ядро является ограничением обычной контейнерной изоляции.

sudo docker run -d --name lab-api-gvisor --runtime=runsc -p 8080:8080 my-app
<img width="1062" height="37" alt="image" src="https://github.com/user-attachments/assets/ceeded79-a589-4efa-844d-b269a8643ffd" />

контейнер запущен:
<img width="874" height="56" alt="image" src="https://github.com/user-attachments/assets/70343a01-2fff-4c3b-bbbc-ec2f723fbbf2" />
Использовалась команда, которая как раз выводит название среды выполнения (runtime), которая используется для работы конкретного контейнера lab-api-gvisor. Можно заметить, что как раз используется runsc
<img width="974" height="40" alt="image" src="https://github.com/user-attachments/assets/b4674835-ffd9-4afe-9e54-fbb23ec7a423" />

**Отличие docker от скрипта**

Собственный скрипт из предыдущих частей использовал низкоуровневые Linux-механизмы: namespaces, cgroups, capabilities и seccomp. Обычный Docker автоматизирует работу с этими механизмами и дополнительно предоставляет управление образами, контейнерами, сетью, портами и volumes.

**Отличие gVisor от docker**

gVisor использует контейнерную модель Docker, но добавляет дополнительный уровень между приложением и ядром Linux хоста. Главным компонентом этого уровня является Sentry.
Из-за этого приложение получает дополнительную границу изоляции, а прямое взаимодействие приложения с ядром хоста уменьшается. Именно это является главным отличием gVisor от обычного Docker runtime.
При этом gVisor не является виртуальной машиной. Отдельного полноценного ядра Linux у контейнера всё равно нет.

**Предел контейнерной изоляции**

Общее ядро является одним из основных пределов контейнерной изоляции. Namespaces, cgroups, capabilities и seccomp позволяют сильно ограничить контейнер, но не превращают его в полностью независимую операционную систему.
gVisor решает эту проблему частично: он не создаёт отдельное ядро, но добавляет ещё один уровень между приложением и ядром хоста. Поэтому его можно рассматривать как более изолированный вариант контейнерного runtime по сравнению с обычным запуском контейнера.






